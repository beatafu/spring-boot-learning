spring:
  config:
    activate:
      on-profile: monitoring

# 监控特定配置
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    metrics:
      enabled: true
    prometheus:
      enabled: true